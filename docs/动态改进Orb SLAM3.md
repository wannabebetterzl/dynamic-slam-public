Huang 等人在 T-ITS 2025 的工作表明，动态场景下单纯依赖语义先验或单纯依赖几何约束都存在局限：前者容易带来高计算开销与误判，后者则在高动态与漏检场景中容易失效。其方法采用轻量目标检测与双向极线/深度几何约束联合筛除动态特征点，本质上体现了“语义候选生成 + 几何一致性验证”的思路。
对我们的研究而言，这一工作的重要启发不在于复现其检测器或具体实现，而在于进一步确认：基础模型输出不应被直接当作最终删点依据，而更适合作为高召回动态候选；真正进入 SLAM 后端的观测，仍需经过几何一致性复核，并根据初始化、跟踪建图和优化的不同阶段采取不同强度的介入策略。
