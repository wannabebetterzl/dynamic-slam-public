[project]
name = "dynamic-object-frontend"
version = "0.1.0"
description = "Foundation-model object observations for Dynamic SLAM"
requires-python = ">=3.10"
dependencies = [
  "numpy",
  "opencv-python",
]

[tool.setuptools.packages.find]
where = ["src"]

